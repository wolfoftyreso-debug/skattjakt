[workspace]
resolver = "2"
members = [
    "crates/core",
    "crates/telemetry",
    "crates/rules",
    "crates/model",
    "crates/gateway",
    "crates/identity",
    "crates/notify",
    "crates/extract",
    "crates/pipeline",
    "crates/simulate",
    "crates/store",
    "crates/jobs",
    "crates/payments",
    "apps/api",
    "apps/analyze",
    "apps/wasm",
    "workers/analysis-worker",
    "workers/notification-worker",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
rust-version = "1.82"
license = "UNLICENSED"
repository = "https://github.com/wolfoftyreso-debug/skattjakt"

[workspace.dependencies]
skattjakt-core = { path = "crates/core" }
skattjakt-telemetry = { path = "crates/telemetry", default-features = false }
skattjakt-rules = { path = "crates/rules" }
skattjakt-model = { path = "crates/model", default-features = false }
skattjakt-gateway = { path = "crates/gateway", default-features = false }
skattjakt-identity = { path = "crates/identity" }
skattjakt-notify = { path = "crates/notify" }
skattjakt-extract = { path = "crates/extract" }
skattjakt-pipeline = { path = "crates/pipeline", default-features = false }
skattjakt-simulate = { path = "crates/simulate" }
skattjakt-store = { path = "crates/store" }
skattjakt-jobs = { path = "crates/jobs" }
skattjakt-payments = { path = "crates/payments" }
skattjakt-api = { path = "apps/api" }
skattjakt-wasm = { path = "apps/wasm", default-features = false }
skattjakt-analyze = { path = "apps/analyze" }

anyhow = "1"
async-trait = "0.1"
axum = { version = "0.8", features = ["multipart", "macros"] }
# `wasmbind` lets `Utc::now()` work on wasm32-unknown-unknown, which is what
# the Vercel function runs. It is target-gated inside chrono, so a native
# build is unaffected.
chrono = { version = "0.4", default-features = false, features = ["std", "clock", "serde", "wasmbind"] }
hex = "0.4"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
sqlx = { version = "0.8", default-features = false, features = [
    "runtime-tokio-rustls",
    "postgres",
    "uuid",
    "chrono",
    "json",
    "migrate",
    "macros",
] }
thiserror = "2"
tokio = { version = "1", features = ["full"] }
tower = "0.5"
tower-http = { version = "0.6", features = ["trace", "limit", "cors"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
# `js` gives v4 a source of randomness on wasm32; target-gated, no effect natively.
uuid = { version = "1", features = ["v4", "serde", "js"] }
tempfile = "3"

[profile.release]
lto = "thin"
codegen-units = 1
strip = "debuginfo"
